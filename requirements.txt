# Requirements for FrankandLopes2025-RefBrain
# Consolidated from the repository notebooks (Fig1/2/3).
# Recommended Python runtime: 3.10
# Install with: pip install -r requirements.txt

# Core scientific stack
numpy>=1.23,<2
pandas>=1.5,<2
scipy>=1.9,<2
matplotlib>=3.5,<4
seaborn>=0.12,<1

# Image processing / IO
scikit-image>=0.19,<1
tifffile>=2022.10
imagecodecs>=2022.10

# 3D rendering (used by Fig2)
pyvista>=0.46,<1
vtk>=9.0,<10

# Notebook / interactive
notebook>=6.5
ipython>=8.0

# Utilities
tqdm>=4.64

# Notes:
# - This is a minimal, conservative pip-style manifest derived from imports used in the notebooks.
# - For pyvista/VTK prefer conda-forge installs to avoid binary build issues on many platforms.