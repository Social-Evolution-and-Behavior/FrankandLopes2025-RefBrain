name: Run notebooks (Fig2 & Fig3)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  execute-notebooks:
    name: Execute Fig2 & Fig3 notebooks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.9
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          # xvfb and Mesa/OpenGL libs help headless rendering for pyvista/vtk
          sudo apt-get install -y xvfb libgl1 libosmesa6

      - name: Install Python dependencies (pip)
        run: |
          python -m pip install --upgrade pip wheel setuptools
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          # Ensure PyVista and VTK are installed (explicit pins to match local run)
          pip install --no-cache-dir pyvista==0.46.3 vtk==9.5.2

      - name: Smoke-test imports
        env:
          MPLBACKEND: Agg
          PYVISTA_OFF_SCREEN: '1'
        run: |
          python - <<'PY'
          import importlib
          packages = ['numpy','pandas','scipy','matplotlib','seaborn','skimage','tifffile','imagecodecs','pyvista','vtk']
          problems = False
          for p in packages:
              try:
                  m = importlib.import_module(p)
                  v = getattr(m, '__version__', 'unknown')
                  print(f'OK: {p} {v}')
              except Exception as e:
                  print(f'ERROR importing {p}: {e}')
                  problems = True
          if problems:
              raise SystemExit('One or more imports failed')
          PY

      - name: Execute Fig2 notebook (headless)
        env:
          MPLBACKEND: Agg
          PYVISTA_OFF_SCREEN: '1'
        run: |
          # Use xvfb to provide a headless X server for VTK/PyVista
          xvfb-run -s "-screen 0 1920x1080x24" jupyter nbconvert --to notebook --execute FrankandLopes_refbrain_code_fig2_suppfig2.ipynb --ExecutePreprocessor.timeout=600 --output executed_Fig2.ipynb

      - name: Execute Fig3 headless script
        env:
          MPLBACKEND: Agg
          PYVISTA_OFF_SCREEN: '1'
        run: |
          # Prefer running the stable headless script (avoids notebook execution order issues)
          xvfb-run -s "-screen 0 1920x1080x24" /usr/bin/env python3 scripts/run_fig3_headless.py

      - name: Upload generated figures
        uses: actions/upload-artifact@v4
        with:
          name: figures
          path: figures

      - name: (optional) Upload executed Fig2 notebook
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: executed-fig2-notebook
          path: executed_Fig2.ipynb
